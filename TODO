# ========================================================================
# TODO list
# ========================================================================
# 
# ----------------------------------------------------------------------
# Todo for 0.04
# ----------------------------------------------------------------------
# DONE    - review table building method for more flexibility
# IN PROG - review general structure of the plugin
# DONE    - performance enhancement by prefering snmp table/bulk request
# DONE    - add STP info
# IN PROG - extend debug if needed
# DONE    - add options for hp, or other brands if relevant
# DONE    - better handling of counters cleared manually or via node restart
#            -> check if enough now
# IN PROG - better non regression testing 
#            -> test phase would include tests on simulated type of monitored nodes
# DONE    - convert network masks to quad-dotted decimal representation (*/[0-32])
#           to free space in the interface table
# DONE    - correctly handle devices where interfaces have the same ifdescr
# DONE    - when interfaces have the same ifdescr, give the suffix choice between 
#           idx or mac addr
# DONE    - add Juniper Netscreen support (+ special columns)
# DONE    - look review (buttons, html structure, css)
# DONE    - detect 32/64 bits counter use changes
# DONE    - detect release change
#         - add a 3rd table in the html page, summarizing the following options: 
#               * inclusions/exclusions of any kind
#               * thresholds of any kind
#         - add a 4th table in the html page, giving some specific device info,
#           depending on the nodetype option
#         - update supported nodes list
#         - create a more detailed documentation on supported nodes, also to prevent
#           regressions
#         - review generated link [details] to be not a relative URL, to be usable in 
#           email notifications
#         - evaluate a system where properties changing from empty to a value are not
#           reported as a problem
# DONE    - add snmp transport domain selection
#
# ----------------------------------------------------------------------
# Todo for 0.05
# ----------------------------------------------------------------------
#         - multiple spanning tree support (currently only default vlan for cisco). Maybe 
#           use stpxInconsistentState?
#         - add a feature to anonymize the info in debug reports (script or option?)
#         - review normalization for a more standard one available in perl and php
#         - add support of Fabric OS Firmware < v5.3.0 (Brocade) 
#
# ----------------------------------------------------------------------
# Todo not classified yet
# ----------------------------------------------------------------------
#         - review the netwaysgrapherv2 support + create templates
#         - add support (+ templates) for other graphers (nagiosgraph, perfparse,...)
#         - option to show mac addresses in the interface table
#         - multiple tables (physical interfaces separated from logical interfaces)
#         - change from html files to php interface and datafiles
#         - netscreen - handle secondary IPs
#
# ========================================================================